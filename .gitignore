# Render working files — regenerate from render/run-chain.md, never commit
render/raw/
render/raw-portrait/
render/frames/
render/jobs/
*.mp4

# ...except the finished, encoded legs. They cost real credits to produce and
# would otherwise be lost with this container.
!assets/clips/
!assets/clips/*.mp4

node_modules/
__pycache__/
.DS_Store
.data/
api/config.php
assets/enhanced/
