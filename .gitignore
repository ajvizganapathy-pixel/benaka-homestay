# Render working files — regenerate from render/run-chain.md, never commit
render/raw/
render/raw-portrait/
render/frames/
render/jobs/
*.mp4

# ...except the one real video this site carries: the walk around the property,
# filmed on site. The AI walkthrough clips that used to be excepted here left
# with the scroll world. They are still on disk and still in history at 76bab70
# — `git show 76bab70:assets/clips/leg-01.mp4 > leg-01.mp4` brings one back.
!assets/video/*.mp4

node_modules/
__pycache__/
.DS_Store
.data/
benaka-data/
api/config.php
assets/enhanced/
